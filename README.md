# fpga-memory-controller
This project implements a custom memory controller for internal FPGA Block RAM (BRAM) using Verilog HDL. The controller allows controlled read and write operations to the BRAM with a single request at a time, ensuring proper timing and data integrity.
